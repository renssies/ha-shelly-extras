# Actions (services) exposed by Shelly Extras.
example_action:
  name: Example action
  description: >-
    Placeholder action. Replace with a real Shelly Extras action.
  fields:
    device_id:
      name: Device
      description: The target Shelly device id.
      required: true
      example: "shellyplus1pm-abc123"
      selector:
        text:
    value:
      name: Value
      description: An optional value passed to the action.
      required: false
      example: "on"
      selector:
        text:
